package main
import(
	"encoding/json"
	"io/ioutil"
)

func parseJSON(file string){
	file, err := ioutil.ReadFile(string)
	if err != nil {
		//throw exception
	}
	//decoder := json.NewDecoder(file)
	//decoder.
}